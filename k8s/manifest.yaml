# ============================================================================
# k8s/manifest.yaml — GENERATED FILE
# Source: variants.yaml  |  Generator: scripts/generate_k8s.py
#
# DO NOT EDIT BY HAND. Re-generate with:
#   python3 scripts/generate_k8s.py
#
# Contains: Namespace, ResourceQuota, NetworkPolicy, and one Service +
# Deployment per unique (app, arch) cmake variant in variants.yaml.
#
# PREREQUISITE — create the GHCR pull secret once per cluster:
#   kubectl create secret docker-registry ghcr-pull-secret \
#     --namespace embedded-os \
#     --docker-server=ghcr.io \
#     --docker-username=<github-username> \
#     --docker-password=<github-pat-with-read:packages>
# ============================================================================

---
apiVersion: v1
kind: Namespace
metadata:
  name: embedded-os
  labels:
    name: embedded-os

---
apiVersion: v1
kind: ResourceQuota
metadata:
  name: embedded-os-quota
  namespace: embedded-os
spec:
  hard:
    requests.cpu: "4"
    requests.memory: "2Gi"
    limits.cpu: "8"
    limits.memory: "4Gi"
    pods: "50"

---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: embedded-os-network-policy
  namespace: embedded-os
spec:
  podSelector: {}
  policyTypes:
    - Ingress
    - Egress
  ingress:
    - from:
        - podSelector: {}
  egress:
    - to:
        - podSelector: {}
    - to:
        - namespaceSelector: {}
      ports:
        - protocol: TCP
          port: 53
        - protocol: UDP
          port: 53
---
# Deployment — gcc-app-x86-64
apiVersion: apps/v1
kind: Deployment
metadata:
  name: gcc-app-x86-64
  namespace: embedded-os
  labels:
    app: gcc-app
    arch: x86_64
    variant: release-x86_64
spec:
  replicas: 1
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: gcc-app
      arch: x86_64
  template:
    metadata:
      labels:
        app: gcc-app
        arch: x86_64
        variant: release-x86_64
    spec:
      terminationGracePeriodSeconds: 30
      nodeSelector:
        kubernetes.io/arch: amd64
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
      imagePullSecrets:
        - name: ghcr-pull-secret
      containers:
        - name: gcc-app-x86-64
          image: ghcr.io/techaccel-upskill/gcc-app-x86-64:latest
          imagePullPolicy: Always
          env:
            - name: APP_NAME
              value: "gcc-app-x86-64"
            - name: TARGET_ARCH
              value: "x86_64"
          resources:
            requests:
              memory: "32Mi"
              cpu: "50m"
            limits:
              memory: "128Mi"
              cpu: "250m"
        livenessProbe:
          exec:
            command: ["sh", "-c", "test -f /app/gcc_app"]
          initialDelaySeconds: 10
          periodSeconds: 30
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          exec:
            command: ["sh", "-c", "test -f /app/gcc_app"]
          initialDelaySeconds: 5
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 2
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: false
            capabilities:
              drop: [ALL]
---
# Deployment — gcc-app-arm-cortex-a
apiVersion: apps/v1
kind: Deployment
metadata:
  name: gcc-app-arm-cortex-a
  namespace: embedded-os
  labels:
    app: gcc-app
    arch: arm-cortex-a
    variant: release-arm
spec:
  replicas: 1
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: gcc-app
      arch: arm-cortex-a
  template:
    metadata:
      labels:
        app: gcc-app
        arch: arm-cortex-a
        variant: release-arm
    spec:
      terminationGracePeriodSeconds: 30
      nodeSelector:
        kubernetes.io/arch: arm
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
      imagePullSecrets:
        - name: ghcr-pull-secret
      containers:
        - name: gcc-app-arm-cortex-a
          image: ghcr.io/techaccel-upskill/gcc-app-arm-cortex-a:latest
          imagePullPolicy: Always
          env:
            - name: APP_NAME
              value: "gcc-app-arm-cortex-a"
            - name: TARGET_ARCH
              value: "arm-cortex-a"
          resources:
            requests:
              memory: "32Mi"
              cpu: "50m"
            limits:
              memory: "128Mi"
              cpu: "250m"
        livenessProbe:
          exec:
            command: ["sh", "-c", "test -f /app/gcc_app"]
          initialDelaySeconds: 10
          periodSeconds: 30
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          exec:
            command: ["sh", "-c", "test -f /app/gcc_app"]
          initialDelaySeconds: 5
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 2
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: false
            capabilities:
              drop: [ALL]
---
# Deployment — gcc-app-aarch64
apiVersion: apps/v1
kind: Deployment
metadata:
  name: gcc-app-aarch64
  namespace: embedded-os
  labels:
    app: gcc-app
    arch: aarch64
    variant: release-aarch64
spec:
  replicas: 1
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: gcc-app
      arch: aarch64
  template:
    metadata:
      labels:
        app: gcc-app
        arch: aarch64
        variant: release-aarch64
    spec:
      terminationGracePeriodSeconds: 30
      nodeSelector:
        kubernetes.io/arch: arm64
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
      imagePullSecrets:
        - name: ghcr-pull-secret
      containers:
        - name: gcc-app-aarch64
          image: ghcr.io/techaccel-upskill/gcc-app-aarch64:latest
          imagePullPolicy: Always
          env:
            - name: APP_NAME
              value: "gcc-app-aarch64"
            - name: TARGET_ARCH
              value: "aarch64"
          resources:
            requests:
              memory: "32Mi"
              cpu: "50m"
            limits:
              memory: "128Mi"
              cpu: "250m"
        livenessProbe:
          exec:
            command: ["sh", "-c", "test -f /app/gcc_app"]
          initialDelaySeconds: 10
          periodSeconds: 30
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          exec:
            command: ["sh", "-c", "test -f /app/gcc_app"]
          initialDelaySeconds: 5
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 2
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: false
            capabilities:
              drop: [ALL]
---
# Deployment — sensor-app-x86-64
apiVersion: apps/v1
kind: Deployment
metadata:
  name: sensor-app-x86-64
  namespace: embedded-os
  labels:
    app: sensor-app
    arch: x86_64
    variant: release-x86_64
spec:
  replicas: 1
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: sensor-app
      arch: x86_64
  template:
    metadata:
      labels:
        app: sensor-app
        arch: x86_64
        variant: release-x86_64
    spec:
      terminationGracePeriodSeconds: 30
      nodeSelector:
        kubernetes.io/arch: amd64
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
      imagePullSecrets:
        - name: ghcr-pull-secret
      containers:
        - name: sensor-app-x86-64
          image: ghcr.io/techaccel-upskill/sensor-app-x86-64:latest
          imagePullPolicy: Always
          env:
            - name: APP_NAME
              value: "sensor-app-x86-64"
            - name: TARGET_ARCH
              value: "x86_64"
          resources:
            requests:
              memory: "32Mi"
              cpu: "50m"
            limits:
              memory: "128Mi"
              cpu: "250m"
        livenessProbe:
          exec:
            command: ["sh", "-c", "test -f /app/sensor_app"]
          initialDelaySeconds: 10
          periodSeconds: 30
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          exec:
            command: ["sh", "-c", "test -f /app/sensor_app"]
          initialDelaySeconds: 5
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 2
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: false
            capabilities:
              drop: [ALL]
---
# Deployment — sensor-app-arm-cortex-a
apiVersion: apps/v1
kind: Deployment
metadata:
  name: sensor-app-arm-cortex-a
  namespace: embedded-os
  labels:
    app: sensor-app
    arch: arm-cortex-a
    variant: release-arm
spec:
  replicas: 1
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: sensor-app
      arch: arm-cortex-a
  template:
    metadata:
      labels:
        app: sensor-app
        arch: arm-cortex-a
        variant: release-arm
    spec:
      terminationGracePeriodSeconds: 30
      nodeSelector:
        kubernetes.io/arch: arm
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
      imagePullSecrets:
        - name: ghcr-pull-secret
      containers:
        - name: sensor-app-arm-cortex-a
          image: ghcr.io/techaccel-upskill/sensor-app-arm-cortex-a:latest
          imagePullPolicy: Always
          env:
            - name: APP_NAME
              value: "sensor-app-arm-cortex-a"
            - name: TARGET_ARCH
              value: "arm-cortex-a"
          resources:
            requests:
              memory: "32Mi"
              cpu: "50m"
            limits:
              memory: "128Mi"
              cpu: "250m"
        livenessProbe:
          exec:
            command: ["sh", "-c", "test -f /app/sensor_app"]
          initialDelaySeconds: 10
          periodSeconds: 30
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          exec:
            command: ["sh", "-c", "test -f /app/sensor_app"]
          initialDelaySeconds: 5
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 2
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: false
            capabilities:
              drop: [ALL]
---
# Deployment — sensor-app-aarch64
apiVersion: apps/v1
kind: Deployment
metadata:
  name: sensor-app-aarch64
  namespace: embedded-os
  labels:
    app: sensor-app
    arch: aarch64
    variant: release-aarch64
spec:
  replicas: 1
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: sensor-app
      arch: aarch64
  template:
    metadata:
      labels:
        app: sensor-app
        arch: aarch64
        variant: release-aarch64
    spec:
      terminationGracePeriodSeconds: 30
      nodeSelector:
        kubernetes.io/arch: arm64
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
      imagePullSecrets:
        - name: ghcr-pull-secret
      containers:
        - name: sensor-app-aarch64
          image: ghcr.io/techaccel-upskill/sensor-app-aarch64:latest
          imagePullPolicy: Always
          env:
            - name: APP_NAME
              value: "sensor-app-aarch64"
            - name: TARGET_ARCH
              value: "aarch64"
          resources:
            requests:
              memory: "32Mi"
              cpu: "50m"
            limits:
              memory: "128Mi"
              cpu: "250m"
        livenessProbe:
          exec:
            command: ["sh", "-c", "test -f /app/sensor_app"]
          initialDelaySeconds: 10
          periodSeconds: 30
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          exec:
            command: ["sh", "-c", "test -f /app/sensor_app"]
          initialDelaySeconds: 5
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 2
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: false
            capabilities:
              drop: [ALL]
---
# Service — watchdog-app-x86-64
apiVersion: v1
kind: Service
metadata:
  name: watchdog-app-x86-64
  namespace: embedded-os
  labels:
    app: watchdog-app
    arch: x86_64
spec:
  type: ClusterIP
  selector:
    app: watchdog-app
    arch: x86_64
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
---
# Deployment — watchdog-app-x86-64
apiVersion: apps/v1
kind: Deployment
metadata:
  name: watchdog-app-x86-64
  namespace: embedded-os
  labels:
    app: watchdog-app
    arch: x86_64
    variant: release-x86_64
spec:
  replicas: 1
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: watchdog-app
      arch: x86_64
  template:
    metadata:
      labels:
        app: watchdog-app
        arch: x86_64
        variant: release-x86_64
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8080"
    spec:
      terminationGracePeriodSeconds: 30
      nodeSelector:
        kubernetes.io/arch: amd64
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
      imagePullSecrets:
        - name: ghcr-pull-secret
      containers:
        - name: watchdog-app-x86-64
          image: ghcr.io/techaccel-upskill/watchdog-app-x86-64:latest
          imagePullPolicy: Always
          env:
            - name: APP_NAME
              value: "watchdog-app-x86-64"
            - name: TARGET_ARCH
              value: "x86_64"
          resources:
            requests:
              memory: "32Mi"
              cpu: "50m"
            limits:
              memory: "128Mi"
              cpu: "250m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 15
          periodSeconds: 30
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 10
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 2
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: false
            capabilities:
              drop: [ALL]
---
# Service — watchdog-app-arm-cortex-a
apiVersion: v1
kind: Service
metadata:
  name: watchdog-app-arm-cortex-a
  namespace: embedded-os
  labels:
    app: watchdog-app
    arch: arm-cortex-a
spec:
  type: ClusterIP
  selector:
    app: watchdog-app
    arch: arm-cortex-a
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
---
# Deployment — watchdog-app-arm-cortex-a
apiVersion: apps/v1
kind: Deployment
metadata:
  name: watchdog-app-arm-cortex-a
  namespace: embedded-os
  labels:
    app: watchdog-app
    arch: arm-cortex-a
    variant: release-arm
spec:
  replicas: 1
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: watchdog-app
      arch: arm-cortex-a
  template:
    metadata:
      labels:
        app: watchdog-app
        arch: arm-cortex-a
        variant: release-arm
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8080"
    spec:
      terminationGracePeriodSeconds: 30
      nodeSelector:
        kubernetes.io/arch: arm
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
      imagePullSecrets:
        - name: ghcr-pull-secret
      containers:
        - name: watchdog-app-arm-cortex-a
          image: ghcr.io/techaccel-upskill/watchdog-app-arm-cortex-a:latest
          imagePullPolicy: Always
          env:
            - name: APP_NAME
              value: "watchdog-app-arm-cortex-a"
            - name: TARGET_ARCH
              value: "arm-cortex-a"
          resources:
            requests:
              memory: "32Mi"
              cpu: "50m"
            limits:
              memory: "128Mi"
              cpu: "250m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 15
          periodSeconds: 30
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 10
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 2
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: false
            capabilities:
              drop: [ALL]
---
# Deployment — canbus-app-x86-64
apiVersion: apps/v1
kind: Deployment
metadata:
  name: canbus-app-x86-64
  namespace: embedded-os
  labels:
    app: canbus-app
    arch: x86_64
    variant: release-x86_64
spec:
  replicas: 1
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: canbus-app
      arch: x86_64
  template:
    metadata:
      labels:
        app: canbus-app
        arch: x86_64
        variant: release-x86_64
    spec:
      terminationGracePeriodSeconds: 30
      nodeSelector:
        kubernetes.io/arch: amd64
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
      imagePullSecrets:
        - name: ghcr-pull-secret
      containers:
        - name: canbus-app-x86-64
          image: ghcr.io/techaccel-upskill/canbus-app-x86-64:latest
          imagePullPolicy: Always
          env:
            - name: APP_NAME
              value: "canbus-app-x86-64"
            - name: TARGET_ARCH
              value: "x86_64"
          resources:
            requests:
              memory: "32Mi"
              cpu: "50m"
            limits:
              memory: "128Mi"
              cpu: "250m"
        livenessProbe:
          exec:
            command: ["sh", "-c", "test -f /app/canbus_app"]
          initialDelaySeconds: 10
          periodSeconds: 30
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          exec:
            command: ["sh", "-c", "test -f /app/canbus_app"]
          initialDelaySeconds: 5
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 2
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: false
            capabilities:
              drop: [ALL]
---
# Deployment — canbus-app-arm-cortex-a
apiVersion: apps/v1
kind: Deployment
metadata:
  name: canbus-app-arm-cortex-a
  namespace: embedded-os
  labels:
    app: canbus-app
    arch: arm-cortex-a
    variant: release-arm
spec:
  replicas: 1
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: canbus-app
      arch: arm-cortex-a
  template:
    metadata:
      labels:
        app: canbus-app
        arch: arm-cortex-a
        variant: release-arm
    spec:
      terminationGracePeriodSeconds: 30
      nodeSelector:
        kubernetes.io/arch: arm
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
      imagePullSecrets:
        - name: ghcr-pull-secret
      containers:
        - name: canbus-app-arm-cortex-a
          image: ghcr.io/techaccel-upskill/canbus-app-arm-cortex-a:latest
          imagePullPolicy: Always
          env:
            - name: APP_NAME
              value: "canbus-app-arm-cortex-a"
            - name: TARGET_ARCH
              value: "arm-cortex-a"
          resources:
            requests:
              memory: "32Mi"
              cpu: "50m"
            limits:
              memory: "128Mi"
              cpu: "250m"
        livenessProbe:
          exec:
            command: ["sh", "-c", "test -f /app/canbus_app"]
          initialDelaySeconds: 10
          periodSeconds: 30
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          exec:
            command: ["sh", "-c", "test -f /app/canbus_app"]
          initialDelaySeconds: 5
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 2
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: false
            capabilities:
              drop: [ALL]
---
# Deployment — canbus-app-aarch64
apiVersion: apps/v1
kind: Deployment
metadata:
  name: canbus-app-aarch64
  namespace: embedded-os
  labels:
    app: canbus-app
    arch: aarch64
    variant: release-aarch64
spec:
  replicas: 1
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: canbus-app
      arch: aarch64
  template:
    metadata:
      labels:
        app: canbus-app
        arch: aarch64
        variant: release-aarch64
    spec:
      terminationGracePeriodSeconds: 30
      nodeSelector:
        kubernetes.io/arch: arm64
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
      imagePullSecrets:
        - name: ghcr-pull-secret
      containers:
        - name: canbus-app-aarch64
          image: ghcr.io/techaccel-upskill/canbus-app-aarch64:latest
          imagePullPolicy: Always
          env:
            - name: APP_NAME
              value: "canbus-app-aarch64"
            - name: TARGET_ARCH
              value: "aarch64"
          resources:
            requests:
              memory: "32Mi"
              cpu: "50m"
            limits:
              memory: "128Mi"
              cpu: "250m"
        livenessProbe:
          exec:
            command: ["sh", "-c", "test -f /app/canbus_app"]
          initialDelaySeconds: 10
          periodSeconds: 30
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          exec:
            command: ["sh", "-c", "test -f /app/canbus_app"]
          initialDelaySeconds: 5
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 2
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: false
            capabilities:
              drop: [ALL]
---
# Deployment — gpio-app-x86-64
apiVersion: apps/v1
kind: Deployment
metadata:
  name: gpio-app-x86-64
  namespace: embedded-os
  labels:
    app: gpio-app
    arch: x86_64
    variant: release-x86_64
spec:
  replicas: 1
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: gpio-app
      arch: x86_64
  template:
    metadata:
      labels:
        app: gpio-app
        arch: x86_64
        variant: release-x86_64
    spec:
      terminationGracePeriodSeconds: 30
      nodeSelector:
        kubernetes.io/arch: amd64
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
      imagePullSecrets:
        - name: ghcr-pull-secret
      containers:
        - name: gpio-app-x86-64
          image: ghcr.io/techaccel-upskill/gpio-app-x86-64:latest
          imagePullPolicy: Always
          env:
            - name: APP_NAME
              value: "gpio-app-x86-64"
            - name: TARGET_ARCH
              value: "x86_64"
          resources:
            requests:
              memory: "32Mi"
              cpu: "50m"
            limits:
              memory: "128Mi"
              cpu: "250m"
        livenessProbe:
          exec:
            command: ["sh", "-c", "test -f /app/gpio_app"]
          initialDelaySeconds: 10
          periodSeconds: 30
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          exec:
            command: ["sh", "-c", "test -f /app/gpio_app"]
          initialDelaySeconds: 5
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 2
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: false
            capabilities:
              drop: [ALL]
---
# Deployment — gpio-app-arm-cortex-a
apiVersion: apps/v1
kind: Deployment
metadata:
  name: gpio-app-arm-cortex-a
  namespace: embedded-os
  labels:
    app: gpio-app
    arch: arm-cortex-a
    variant: release-arm
spec:
  replicas: 1
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: gpio-app
      arch: arm-cortex-a
  template:
    metadata:
      labels:
        app: gpio-app
        arch: arm-cortex-a
        variant: release-arm
    spec:
      terminationGracePeriodSeconds: 30
      nodeSelector:
        kubernetes.io/arch: arm
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
      imagePullSecrets:
        - name: ghcr-pull-secret
      containers:
        - name: gpio-app-arm-cortex-a
          image: ghcr.io/techaccel-upskill/gpio-app-arm-cortex-a:latest
          imagePullPolicy: Always
          env:
            - name: APP_NAME
              value: "gpio-app-arm-cortex-a"
            - name: TARGET_ARCH
              value: "arm-cortex-a"
          resources:
            requests:
              memory: "32Mi"
              cpu: "50m"
            limits:
              memory: "128Mi"
              cpu: "250m"
        livenessProbe:
          exec:
            command: ["sh", "-c", "test -f /app/gpio_app"]
          initialDelaySeconds: 10
          periodSeconds: 30
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          exec:
            command: ["sh", "-c", "test -f /app/gpio_app"]
          initialDelaySeconds: 5
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 2
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: false
            capabilities:
              drop: [ALL]
---
# Deployment — gpio-app-aarch64
apiVersion: apps/v1
kind: Deployment
metadata:
  name: gpio-app-aarch64
  namespace: embedded-os
  labels:
    app: gpio-app
    arch: aarch64
    variant: release-aarch64
spec:
  replicas: 1
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: gpio-app
      arch: aarch64
  template:
    metadata:
      labels:
        app: gpio-app
        arch: aarch64
        variant: release-aarch64
    spec:
      terminationGracePeriodSeconds: 30
      nodeSelector:
        kubernetes.io/arch: arm64
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
      imagePullSecrets:
        - name: ghcr-pull-secret
      containers:
        - name: gpio-app-aarch64
          image: ghcr.io/techaccel-upskill/gpio-app-aarch64:latest
          imagePullPolicy: Always
          env:
            - name: APP_NAME
              value: "gpio-app-aarch64"
            - name: TARGET_ARCH
              value: "aarch64"
          resources:
            requests:
              memory: "32Mi"
              cpu: "50m"
            limits:
              memory: "128Mi"
              cpu: "250m"
        livenessProbe:
          exec:
            command: ["sh", "-c", "test -f /app/gpio_app"]
          initialDelaySeconds: 10
          periodSeconds: 30
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          exec:
            command: ["sh", "-c", "test -f /app/gpio_app"]
          initialDelaySeconds: 5
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 2
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: false
            capabilities:
              drop: [ALL]
