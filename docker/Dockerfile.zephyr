# ============================================================================
# Stage 1: Builder
# ============================================================================
FROM ubuntu:24.04 AS builder

RUN apt-get update && apt-get install -y --no-install-recommends \
    build-essential=12.9ubuntu3 \
    cmake=3.28.3-1 \
    python3=3.12.3-0ubuntu1 \
    python3-pip=24.0-1 \
    git=1:2.43.0-1ubuntu7.1 \
    && rm -rf /var/lib/apt/lists/*

RUN pip3 install --no-cache-dir \
    west==1.4.0

WORKDIR /build

COPY apps/zephyr_app/ .

RUN mkdir -p build && \
    cd build && \
    cmake .. && \
    make && \
    test -f zephyr_app || (echo "Build failed: zephyr_app binary not found" && exit 1)

# ============================================================================
# Stage 2: Runtime
# ============================================================================
FROM ubuntu:24.04

RUN apt-get update && apt-get install -y --no-install-recommends \
    libc6=2.39-0ubuntu8.3 \
    && rm -rf /var/lib/apt/lists/* \
    && apt-get clean

# Create non-root user for security
RUN useradd -m -u 1000 embedded && \
    mkdir -p /app && \
    chown -R embedded:embedded /app

WORKDIR /app
USER embedded

# Copy only the built binary from builder stage
COPY --from=builder --chown=embedded:embedded /build/build/zephyr_app .

EXPOSE 8080

HEALTHCHECK --interval=30s --timeout=5s --start-period=10s --retries=3 \
    CMD test -f /app/zephyr_app || exit 1

CMD ["./zephyr_app"]